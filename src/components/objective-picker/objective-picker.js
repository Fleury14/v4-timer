// @flow
import React from 'react';
import './objective-picker.scss';

type Props = {
    id: number
}

const ObjectivePicker = (props: Props) => {
    return (
        <p>Pick an objective plx</p>
    );
}

export default ObjectivePicker;